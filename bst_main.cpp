#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	
	allocate(P)
	address root
	insertNode(root,n,x)
	PrintInorder(root,n,x)
	
	return 0;
}
